import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_chat/app/modules/new_sala/new_sala_page.dart';

main() {
  testWidgets('NewSalaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewSalaPage(title: 'NewSala')));
    //  final titleFinder = find.text('NewSala');
    //  expect(titleFinder, findsOneWidget);
  });
}
